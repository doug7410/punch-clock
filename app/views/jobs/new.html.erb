
<%= render 'shared/errors', object: @job %>

<%= form_for(@job) do |f| %>
  
<div>
  <%= f.label :name %>
  <%= f.text_field :name %>
</div>
<div>
  <%= f.label :description %>
  <%= f.text_area :description %>
</div>
<%= f.submit 'Add Job' %>    
<% end %>
  